import {cat_enums} from "../utils/consts.js"

const category = [
    cat_enums.UNDERWEIGHT,
    cat_enums.NORMAL_WEIGHT,
    cat_enums.OVERWEIGHT,
    cat_enums.MODERATELY_OBESE,
    cat_enums.SEVERELY_OBESE,
    cat_enums.VERY_SEVERELY_OBESE
]
export default category
